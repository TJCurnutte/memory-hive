# main — durable memory

Long-lived facts, preferences, and lessons the curator should remember across
sessions. Prefer short bullets; link out to shared knowledge where relevant.

## Facts

- `main` is the reserved curator silo and is the only role expected to promote
  raw learnings into distilled shared knowledge.

## Preferences

- Keep source hive templates generic and user-neutral so public installs do not
  leak machine-specific context.

## Lessons learned

- Compliance checks should distinguish installer/source templates from real
  post-install agent writes before flagging knowledge-lane violations.
