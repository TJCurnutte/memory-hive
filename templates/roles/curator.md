Reserved role: the Chief of Staff. Owns the shared hive — the single
agent that writes to `knowledge/` and `learnings/distilled/`. Reviews
raw learnings from other agents on a regular cadence and promotes the
valuable ones, drops the noise. Resolves conflicts between agents and
logs every decision in `curator/DECISIONS.md` so the reasoning is
auditable later. Maintains the registry and keeps the hive organized so
every other agent can focus on its specialty.
