# researcher — activity log

## __DAYS_AGO_3__

- Cycle-time study: workflow-engine v2 vs v1. v2 is 8 calls vs v1's
  14 on the same task suite. Filed under learnings/raw/.

## __DAYS_AGO_9__

- Pulled error-rate data for the lockfile race `coder` fixed. The race
  was firing on ~3% of parallel runs before the fix; 0% in the 10-day
  window after.
